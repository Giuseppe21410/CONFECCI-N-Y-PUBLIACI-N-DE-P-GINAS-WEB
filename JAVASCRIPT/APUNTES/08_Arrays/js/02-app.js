// como aceder a los elementos de array

//creamos uno llamado "numeros"

const numeros = [10 ,20, 30, 40, 50, 60];
console.table(numeros);


//los elementos que hay en un array se acceden utilizando su indice
//importante en Js el indice comienza en 0 
//si accedemos al primer elemento 
console.log(numeros[0]);

console.log(numeros[1]);

console.log(numeros[2]);

console.log(numeros[3]);

console.log(numeros[4]);

console.log(numeros[5]);